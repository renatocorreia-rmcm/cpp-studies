#include <iostream>
using namespace std;

int main(){

    //fix array has its memory addresses stored side by side, so its really fast to access those addresses
    //thats why the size of an array must be known before the compilation

    return 0;
}